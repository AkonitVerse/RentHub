-- Удаляем Client.address: для розничной аренды постоянный адрес клиента
-- не нужен, адрес доставки указывается при оформлении каждого заказа.
-- Order.address (адрес конкретного заказа) сохраняется.
ALTER TABLE "clients" DROP COLUMN IF EXISTS "address";
