-- Удаление поля «заметка для менеджера» у клиента: для розничной аренды
-- физ.лицам это поле не нужно. Order.notes (комментарий к заказу) сохраняется.
ALTER TABLE "clients" DROP COLUMN IF EXISTS "notes";
